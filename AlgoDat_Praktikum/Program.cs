using System;
using AlgoDat_Praktikum.Code;

namespace AlgoDat_Praktikum
{
    /// <summary>
    /// Only use to interact with user and its inputs.
    /// Own implementations are handled in own classes.
    /// If you add code here create an own region and consider merge conflicts.
    /// </summary>
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
            Console.ReadKey();
        }
    }
}
